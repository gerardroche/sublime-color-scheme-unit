{
    // Enable coverage report.
    "color_scheme_unit.coverage": false,

    // Enable console debug messages.
    "color_scheme_unit.debug": false,

    // Results output. Valid values are "view" or "panel"
    "color_scheme_unit.strategy": "panel"
}
